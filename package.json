{
  "name": "electron-vr-overlay-api",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript + ESM Electron demo with a node-gyp VR overlay bridge scaffold.",
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "build": "node ./scripts/build.mjs",
    "build:addon": "node-gyp rebuild",
    "rebuild:electron": "node ./scripts/rebuild-electron.mjs",
    "build:all": "npm run build && npm run rebuild:electron",
    "test:e2e": "node --test ./tests/e2e/mock-preview.test.mjs",
    "start": "npm run build && npm run rebuild:electron && electron .",
    "start:no-sandbox": "npm run build && npm run rebuild:electron && electron . --no-sandbox",
    "dev": "npm run start"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "electron": "^37.6.0",
    "node-gyp": "^11.5.0",
    "playwright": "^1.55.0",
    "typescript": "^5.9.3"
  }
}
